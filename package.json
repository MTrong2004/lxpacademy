{
  "name": "learning-hub-turso-vercel",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "migrate": "node scripts/migrate.js",
    "map": "node scripts/map.js",
    "find": "node scripts/find-symbol.js",
    "check:catch": "node scripts/check-empty-catch.js",
    "check:overrides": "node scripts/check-overrides.js",
    "format": "prettier --write \"src/**/*.js\"",
    "format:check": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "@libsql/client": "^0.17.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "prettier": "^3.9.6"
  }
}
